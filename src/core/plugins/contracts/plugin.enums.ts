export enum PluginStatus {
  Installed = 'installed',
  Enabled = 'enabled',
  Disabled = 'disabled',
  Errored = 'errored',
  Updating = 'updating',
  Removed = 'removed',
}

export enum PluginScope {
  Guild = 'guild',
  Global = 'global',
}
