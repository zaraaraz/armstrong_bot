name: Setup Node + Cache
description: Setup Node 22 and restore npm cache (checkout must be done before calling this action)

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: npm

    - name: Install dependencies
      shell: bash
      run: npm ci
